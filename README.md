# Sentiment-analysis
Sentiment Analysis on input text/files using pretrained RoBERTa

# What is it?
It allows user to select a file to exctract the sentiment from dataset of customers reviews

# Prerequisite
Ensure the dataset has a column title named review

# Usage
To run the GUI, please run the following command on Windows
python main.ipynb
